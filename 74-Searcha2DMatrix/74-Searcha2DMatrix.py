        while l<=r:
            m = (l+r)//2
            if list1[m] == target:
                return True
            elif list1[m] > target:
                r = m - 1
        r = len(list1) -1
        l = 0
                break
                list1 = row
            if row[-1] >= target:
        for row in matrix:
        prev = 0
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
class Solution:
        list1 = []
[
