

            temp.append(k)
        
                k.append(matrix[n-j-1][i])
        for i in range(n): # 0,1,2
            k = []
            for j in range(n): # 0,1,2
        n = len(matrix)

        """

        temp = []
class Solution:
    def rotate(self, matrix: List[List[int]]) -> None:
        """
        Do not return anything, modify matrix in-place instead.


        for i in range(n): 
            for j in range(n):
                matrix[i][j] = temp[i][j]

