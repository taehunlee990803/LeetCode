            # dic[items[i][0]].append(items[i][1])
            # dic[id].append(score)
            else:
                dic[id] = [score]
        ans = []
        for element in dic:
            for score in dic[element]:
            sum = 0
                container.append(score)
            score = items[i][1]
            if id in dic:
                dic[id].append(score)
            id = items[i][0]
        for i in range(len(items)):
        print(dic)
            container = []
            container.sort()
            sum += (container[-1] + container[-2] + container[-3] + container[-4] + container[-5])
            temp = [element, int(sum/5)]
        dic = {}
class Solution:
    def highFive(self, items: List[List[int]]) -> List[List[int]]:
        items.sort()
[
