










            elif num <= second:
                second = num
            else:
        for num in nums:
            if num <= first:
                first = num
    def increasingTriplet(self, nums: List[int]) -> bool:
        first = float('inf')
        second = float('inf')

class Solution:
                return True
        return False
