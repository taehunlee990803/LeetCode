class Solution:
    def maximum69Number (self, num: int) -> int:
        temp = str(num)
       
        ans = ""
        
        for i in range(len(temp)):
            if temp[i] == '6':
        return int(ans)
                ans += "9"
       
                ans += temp[i+1:len(temp)]
            ans += "9"
                return int(ans)
        # return int(temp)
9669
