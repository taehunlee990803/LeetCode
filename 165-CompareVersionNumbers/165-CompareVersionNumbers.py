            for i in range(len(left)):
                if left[i] > right[i]:
        elif len(left) < len(right):
            else:
                return 0
                    return 1
                elif left[i] < right[i]:
                    return -1
            if sum(right) - sum(left) > 0:
                return -1
            else:
                return 0
        else:
            for i in range(len(left)):
                if left[i] > right[i]:
                    return 1
"
