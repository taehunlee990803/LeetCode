
        nums = sorted(nums)
        while nums[temp] > 0 or temp >= 0:
        return -1
        temp = len(nums)-1

            minus = nums[temp]*-1
            if minus in nums:
                return nums[temp]
            else:
                temp -= 1
        # print(nums)

        if nums[0] > 0:
            return -1
[
