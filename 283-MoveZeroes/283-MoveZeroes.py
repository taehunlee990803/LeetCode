











                        # nums[i] = nums[j]
                        # nums[j] = temp
                        # print(nums)
                            break
                            nums[j] = temp
                            nums[i] = nums[j]
                        if nums[j] != 0:
                    for j in range(i+1, len(nums)):
                    temp = nums[i]
                if nums[i] == 0: 
                # print(nums)
            for i in range(len(nums)):
        else:
