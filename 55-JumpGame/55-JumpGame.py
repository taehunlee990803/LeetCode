        max_distance = 0
        while loc <= max_distance:  # Change the condition to handle the case where loc exceeds max_distance
                return True

            max_distance = max(max_distance, temp)
            loc += 1

            temp = loc + nums[loc]

            if temp >= destination:
        return False
[2,3,1,1,4]
