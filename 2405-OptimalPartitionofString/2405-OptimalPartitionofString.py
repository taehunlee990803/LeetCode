        container = [[]]
        for element in s:
            if element in container[index]:
            check = False
        index = 0
                index += 1
            else:
                container.append([element])
    def partitionString(self, s: str) -> int:
class Solution:
                container[index].append(element)
     
        return len(container)


        
"
