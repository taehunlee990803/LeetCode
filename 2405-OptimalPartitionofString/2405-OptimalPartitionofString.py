        # return ans 
        #     ans = max(dic[element], ans)
        container = [[]]
        for element in s:
            if element in container[index]:
            check = False
        index = 0
                index += 1
            else:
                container.append([element])
        # for element in dic:
        # ans = 0
        #     else:
        #         dic[element] = 1
        # for element in s:
        #     if element in dic:
        #         dic[element] += 1
        # dic = {}
        # container = []
        # temp_container = []
class Solution:
    def partitionString(self, s: str) -> int:
"
