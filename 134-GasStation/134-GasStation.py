          
        #     for j in range(0,length-1): #12345
        #         next_idx = i + j
        #         next_next_idx = i + j + 1
        #         if next_idx >= length: 
        #             next_idx -= length
        #         if next_next_idx >= length: 
        #         minimum_gas = cost[i-1]
        #     temp = True
        #     if i - 1 < 0:
        #         minimum_gas = cost[length - 1]
        #     else:
        # for i in range(length):#01234
        #     current = gas[i]
    def canCompleteCircuit(self, gas: List[int], cost: List[int]) -> int:
        # length = len(gas)
        # ans = -1
class Solution:
[1,2,3,4,5]
[
