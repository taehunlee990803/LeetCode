            fast = fast.next.next
        while fast and fast.next:
            # prev = slow
            slow = slow.next
        prev = ListNode(0)

        curr = head
        
        fast = head
[ListNode]:
        if not head or not head.next:
            return None
#         self.next = next
class Solution:
    def deleteMiddle(self, head: Optional[ListNode]) -> Optional
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
        prev.next = head
        

        slow = prev
