    def reverseVowels(self, s: str) -> str:
        vowels = ['a', 'e','i','o','u']




class Solution:
        for element in s :
            if element in vowels or element.lower() in vowels:
        temp = []
                temp.append(element)
        for i in range(len(s)):
            if s[i] in vowels or  s[i].lower() in vowels:
                # s[i] = temp[k]
        k = len(temp)-1
                k-=1
        return ans 
        ans = ""
            else:
                ans += s[i]
                ans += temp[k]
        print(temp)
