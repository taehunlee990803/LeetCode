class Solution:
    def findMatrix(self, nums: List[int]) -> List[List[int]]:
        return container
          


        dic = Counter(nums)
        if len(dic) == len(nums):
            return [nums]
        for element in dic:
            count = dic[element]
        container = []
            if len(container) < count:
            else:
                for i in range(0,count):
                    container[i].append(element)
                for i in range(len(container)):
                    container[i].append(element)
                dif = count - len(container)
                for j in range(dif):
                    temp = [element]
                    container.append(temp)
        
[
