            if zero > one:
                else:
                    one += 1
            for j in range(n):
                if grid[j][i] == 0:
                    zero += 1
                for t in range(n):
                    if grid[t][i] == 0:
                        grid[t][i] = 1
                    else:
            one = 0
            zero = 0
                        grid[i][j] = 0
        for i in range(k):
                        grid[t][i] = 0
[
