            # firstLetter = words[i][0]
        for i in range(n):

            container[element] = i
        # print(container)
        n = len(words)
        container = {}
        for i,element in enumerate(order):


    def isAlienSorted(self, words: List[str], order: str) -> bool:
            return False
                return True
        if len(current) > len(next):
        else:
            return True
            for j in range(i+1, n):
                if words[i] != words[j]:
                    if not self.is_valid(words[i],words[j],container):
                        return False
        return True
            #     currentFirst = words[j][0]
            #     if container[firstLetter] > container[currentFirst]:
            #         return False
            #     elif container[firstLetter] == container[currentFirst]:
[
